#include "main.h"

/**
 * _strlen - check the code
 * description - this program swap two numbers
 * @s: pointer s
 * Return: 0.
 */
int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{

}
return (i);
}
