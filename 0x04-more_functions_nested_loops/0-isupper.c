#include "main.h"

/**
 * _isupper - the main entry
 * Description - this program returns 1 and 0
 * @c : a variable
 * Return: 0
*/

int _isupper(int c)
{
if ((c >= 65) && (c <= 90))
{
return (1);
}
else
{
return (0);
}
}
