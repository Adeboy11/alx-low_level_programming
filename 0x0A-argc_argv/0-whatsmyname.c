#include <stdio.h>
#include <stdlib.h>

/**
 * main - the name of the file
 * @argc: argument
 * @argv: argument
 * Return: 0
 */

int main(int argc, int *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
