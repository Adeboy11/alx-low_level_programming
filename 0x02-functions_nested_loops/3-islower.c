#include "main.h"

/**
 * _islower - the entry point
 * Description - a program that check for lower letter
 * Return: 0
*/

int main(void)
{
char c;

if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
