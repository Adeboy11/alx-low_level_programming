#include <main.h>

/**
 * _abs - a program that check the absolute no
 * Description - this program checks absolute value
 * @ab: ab is the variable
 *Return: 0
 */

int _abs(int ab)
{
return (ab * ((ab > 0) - (ab < 0)));
}
