#include <stdio.h>
/**
 * main - Entry point
 * Description: a program that prints alphabet
 * Return: 0
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
		putchar(a);
	putchar(\n);
	return (0);
}
