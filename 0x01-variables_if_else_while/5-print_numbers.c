#include <stdio.h>
/**
 * main - entry point
 * Description: program to input all alphats
 * Return: 0
 */
int main(void)
{
        int n;

        for (n = 0; x < 10; n++)
                printf("%d", n);
        putchar('\n');
        return (0);
}
